<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.meicorl.shopping_mall_miniapp.mybatis.dao.MerchantDao">
    <select id="getMerchants" resultType="com.meicorl.shopping_mall_miniapp.mybatis.pojo.Merchant">
        select id, merchant_name, logo, description, building, floor
        from t_merchant_info
        where building = #{building} and floor = #{floor} and merchant_type = 1
    </select>
    <select id="getMerchantEvaluations" resultType="com.meicorl.shopping_mall_miniapp.mybatis.pojo.Evaluation">
        select * id, merchant_id, comment, create_time
        from t_evalution_info
        where merchant_id = #{merchant_id}
        order by create_time desc
        limit #{offset}, #{limit}
    </select>
</mapper>